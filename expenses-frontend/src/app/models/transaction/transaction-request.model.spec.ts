import { TransactionRequest } from './transaction-requestmodel';

describe('TransactionRequest', () => {
  it('should create an instance', () => {
    expect(new TransactionRequest()).toBeTruthy();
  });
});
