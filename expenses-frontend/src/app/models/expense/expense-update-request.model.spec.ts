import { ExpenseUpdateRequest } from './expense-update-request.model';

describe('ExpenseUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new ExpenseUpdateRequest()).toBeTruthy();
  });
});
