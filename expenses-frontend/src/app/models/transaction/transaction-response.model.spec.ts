import { TransactionResponse } from './transaction-responsemodel';

describe('TransactionResponse', () => {
  it('should create an instance', () => {
    expect(new TransactionResponse()).toBeTruthy();
  });
});
